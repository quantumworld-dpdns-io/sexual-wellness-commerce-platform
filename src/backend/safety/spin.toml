spin_manifest_version = 2

[application]
name = "safety-service"
version = "0.1.0"

[[trigger.http]]
route = "/..."
component = "safety"

[component.safety]
source = "target/wasm32-wasip1/release/safety.wasm"
allowed_outbound_hosts = []
